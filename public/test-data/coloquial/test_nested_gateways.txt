This process describes test_nested_gateways.

The process begins when Start by Process.
After this (begin), the process moves to Outer Split.
The process splits into parallel paths at Outer Split (Process).
  - One path goes to Branch A Start (fork_a).
  Next, Branch A Start (LaneA) performs a task.
  After this (prepare), the process moves to Inner Decision.
  At Inner Decision (LaneA), a decision is made:
    - The process continues to Inner Path1 (path1) if option == 1.
    Next, Inner Path1 (LaneA) performs a task.
    After this (complete1), the process moves to Inner Join.
    The parallel paths join at Inner Join (LaneA).
    After this (merge), the process moves to Branch A End.
      Next, Branch A End (LaneA) performs a task.
      After this (complete_a), the process moves to Outer Join.
      The parallel paths join at Outer Join (Process).
      After this (sync), the process moves to End.
    - The process continues to Inner Path2 (path2) if option == 2.
    Next, Inner Path2 (LaneA) performs a task.
    After this (complete2), the process moves to Inner Join.
    The parallel paths join at Inner Join (LaneA).
    After this (merge), the process moves to Branch A End.
      Next, Branch A End (LaneA) performs a task.
      After this (complete_a), the process moves to Outer Join.
      The parallel paths join at Outer Join (Process).
      After this (sync), the process moves to End.
  - One path goes to Branch B (fork_b).
  Next, Branch B (LaneB) performs a task.
  After this (complete_b), the process moves to Outer Join.
  The parallel paths join at Outer Join (Process).
  After this (sync), the process moves to End.

The process completes when End is reached.