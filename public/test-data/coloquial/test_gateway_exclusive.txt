This process describes test_gateway_exclusive.

The process begins when Start by Process.
After this (begin), the process moves to Check Condition.
At Check Condition (Process), a decision is made:
  - The process continues to Path A (condition_true) if condition == true.
  Next, Path A (Process) performs a task.
  After this (complete_a), the process moves to Gateway Join.
  The parallel paths join at Gateway Join (Process).
  After this (merge), the process moves to End.
  - The process continues to Path B (condition_false) if condition == false.
  Next, Path B (Process) performs a task.
  After this (complete_b), the process moves to Gateway Join.
  The parallel paths join at Gateway Join (Process).
  After this (merge), the process moves to End.

The process completes when End is reached.