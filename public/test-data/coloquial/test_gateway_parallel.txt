This process describes test_gateway_parallel.

The process begins when Start by Process.
After this (begin), the process moves to Parallel Split.
The process splits into parallel paths at Parallel Split (Process).
  - One path goes to Task A (fork_a).
  Next, Task A (LaneA) performs a task.
  After this (complete_a), the process moves to Parallel Join.
  The parallel paths join at Parallel Join (Process).
  After this (sync), the process moves to End.
  - One path goes to Task B (fork_b).
  Next, Task B (LaneB) performs a task.
  After this (complete_b), the process moves to Parallel Join.
  The parallel paths join at Parallel Join (Process).
  After this (sync), the process moves to End.
  - One path goes to Task C (fork_c).
  Next, Task C (LaneC) performs a task.
  After this (complete_c), the process moves to Parallel Join.
  The parallel paths join at Parallel Join (Process).
  After this (sync), the process moves to End.

The process completes when End is reached.