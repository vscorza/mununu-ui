This process describes test_gateway_inclusive.

The process begins when Start by Process.
After this (begin), the process moves to Inclusive Split.
At Inclusive Split (Process), one or more paths can be taken:
  - The process continues to Path1 (path1) if condition1 == true.
  Next, Path1 (Lane1) performs a task.
  After this (complete1), the process moves to Inclusive Join.
  The parallel paths join at Inclusive Join (Process).
  After this (merge), the process moves to End.
  - The process continues to Path2 (path2) if condition2 == true.
  Next, Path2 (Lane2) performs a task.
  After this (complete2), the process moves to Inclusive Join.
  The parallel paths join at Inclusive Join (Process).
  After this (merge), the process moves to End.
  - The process continues to Path3 (path3) if condition3 == true.
  Next, Path3 (Lane3) performs a task.
  After this (complete3), the process moves to Inclusive Join.
  The parallel paths join at Inclusive Join (Process).
  After this (merge), the process moves to End.

The process completes when End is reached.