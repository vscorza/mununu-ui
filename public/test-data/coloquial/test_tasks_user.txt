This process describes test_tasks_user.

The process begins when Start by User.
After this (start_review), the process moves to Review Document.
Next, Review Document (User) requires user action.
After this (submit_for_approval), the process moves to Approve Request.
Next, Approve Request (Manager) requires user action.
After this (approved), the process moves to End.

The process completes when End is reached.