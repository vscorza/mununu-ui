// SYNTCOMP: Lily Demo V23
// TLSF STATUS: realizable
// Inputs:  e
// Outputs: s
// States:  4  (signal-state encoding, 2^2)
// Generated by tools/tlsf_to_ctxdsl.py

context syntcomp_lily_demo_v23 {
    alphabet {
        label set_e;
        label clr_e;
        label set_s;
        label clr_s;
    }

    automata {
        automaton Signals {
            controllable {
                label set_s;
                label clr_s;
            }
            states {
                state v00 initial;
                state v01;
                state v10;
                state v11;
            }
            transitions {
                transition v00 -> v10 on label set_e;
                transition v00 -> v00 on label clr_e;
                transition v00 -> v01 on label set_s;
                transition v00 -> v00 on label clr_s;
                transition v01 -> v11 on label set_e;
                transition v01 -> v01 on label clr_e;
                transition v01 -> v01 on label set_s;
                transition v01 -> v00 on label clr_s;
                transition v10 -> v10 on label set_e;
                transition v10 -> v00 on label clr_e;
                transition v10 -> v11 on label set_s;
                transition v10 -> v10 on label clr_s;
                transition v11 -> v11 on label set_e;
                transition v11 -> v01 on label clr_e;
                transition v11 -> v11 on label set_s;
                transition v11 -> v10 on label clr_s;
            }
        }
    }

    mu_formulas {
        formula syntcomp_prop {
            over Signals;
            body = ((! ((nu NuG2. (((! ((v10 || v11) && (([] (v01 || v11))))) || (([] (v10 || v11)))) && ([] NuG2))) && (nu NuG3. ((mu MuF4. ((! (v10 || v11)) || (<> MuF4))) && ([] NuG3)))))
            || (nu NuG1. ((([] ((! (v10 || v11)) && (([] (! (v01 || v11))))))) && ([] NuG1))));
        }
    }

    controllers {
        controller synth {
            source Signals;
            satisfying syntcomp_prop;
        }
    }
}
